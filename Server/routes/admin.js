const express = require('express');
const router = express.Router();
const mongoose = require('mongoose')



router.get('/api/admin', (req, res)=>{
          
})


router.post('/api/admin/create-post', (req, res)=>{

})